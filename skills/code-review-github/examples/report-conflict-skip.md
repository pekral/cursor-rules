# Example: PR Skipped Due to Conflicts

## PR #105 — refactor(payments): extract gateway adapter

**Code review skipped.** The PR has merge conflicts with the base branch (`main`). Resolve the conflicts and request a new review.

### Conflicting files

- `src/Services/PaymentService.php`
- `config/gateways.php`
