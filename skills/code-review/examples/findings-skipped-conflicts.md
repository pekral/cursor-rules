# Example: CR Skipped Due to Conflicts

Code review was **skipped** for PR #87 — the PR has merge conflicts with the base branch.

**Next action:** Resolve the merge conflicts and request a new code review.
